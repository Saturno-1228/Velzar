from telegram import Update
from telegram.ext import ContextTypes

# --- HERE GO ADMIN COMMANDS (Ban, Mute, Purge, etc.) ---

async def ban_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Placeholder
    pass
